# Alogrithm Cloud 微服务应用部署配置
# 包含：Gateway、User、Post、File、Mail、Notification、Search、Log、AI 服务
# 使用方式：docker-compose up -d

version: '3.8'

services:
  # API 网关
  algorithm-gateway:
    build:
      context: .
      dockerfile: algorithm-gateway/Dockerfile
    image: algorithm-gateway:${ALOGRITHM_CLOUD_VERSION:-latest}
    container_name: algorithm-gateway
    restart: always
    ports:
      - "${GATEWAY_PORT_EXTERNAL}:8080"
    environment:
      - SPRING_PROFILES_ACTIVE=prod
      - SPRING_CLOUD_NACOS_CONFIG_SERVER_ADDR=${NACOS_HOST}:${NACOS_PORT}
      - SPRING_CLOUD_NACOS_DISCOVERY_SERVER_ADDR=${NACOS_HOST}:${NACOS_PORT}
      - SPRING_CLOUD_NACOS_USERNAME=${NACOS_USERNAME}
      - SPRING_CLOUD_NACOS_PASSWORD=${NACOS_PASSWORD}
      - SPRING_MAIN_LAZY_INITIALIZATION=true
      - JAVA_TOOL_OPTIONS=-Xss256k -XX:MaxMetaspaceSize=128m -XX:MaxRAMPercentage=60.0 -XX:+TieredCompilation -XX:TieredStopAtLevel=1 -XX:+UseSerialGC -Dnacos.logging.default.config.enabled=false
    deploy:
      resources:
        limits:
          memory: 448M
    networks:
      - algorithm-network

  # 用户服务
  algorithm-user-service:
    build:
      context: .
      dockerfile: algorithm-service/algorithm-user-service/Dockerfile
    image: algorithm-user-service:${ALOGRITHM_CLOUD_VERSION:-latest}
    container_name: algorithm-user-service
    restart: always
    ports:
      - "${USER_SERVICE_PORT_EXTERNAL}:8081"
    environment:
      - SPRING_PROFILES_ACTIVE=prod
      - SPRING_CLOUD_NACOS_CONFIG_SERVER_ADDR=${NACOS_HOST}:${NACOS_PORT}
      - SPRING_CLOUD_NACOS_DISCOVERY_SERVER_ADDR=${NACOS_HOST}:${NACOS_PORT}
      - SPRING_CLOUD_NACOS_USERNAME=${NACOS_USERNAME}
      - SPRING_CLOUD_NACOS_PASSWORD=${NACOS_PASSWORD}
      - SPRING_MAIN_LAZY_INITIALIZATION=true
      - JAVA_TOOL_OPTIONS=-Xss256k -XX:MaxMetaspaceSize=128m -XX:MaxRAMPercentage=60.0 -XX:+TieredCompilation -XX:TieredStopAtLevel=1 -XX:+UseSerialGC -Dnacos.logging.default.config.enabled=false
    deploy:
      resources:
        limits:
          memory: 448M
    networks:
      - algorithm-network

  # 帖子服务
  algorithm-post-service:
    build:
      context: .
      dockerfile: algorithm-service/algorithm-post-service/Dockerfile
    image: algorithm-post-service:${ALOGRITHM_CLOUD_VERSION:-latest}
    container_name: algorithm-post-service
    restart: always
    ports:
      - "${POST_SERVICE_PORT_EXTERNAL}:8082"
    environment:
      - SPRING_PROFILES_ACTIVE=prod
      - SPRING_CLOUD_NACOS_CONFIG_SERVER_ADDR=${NACOS_HOST}:${NACOS_PORT}
      - SPRING_CLOUD_NACOS_DISCOVERY_SERVER_ADDR=${NACOS_HOST}:${NACOS_PORT}
      - SPRING_CLOUD_NACOS_USERNAME=${NACOS_USERNAME}
      - SPRING_CLOUD_NACOS_PASSWORD=${NACOS_PASSWORD}
      - SPRING_MAIN_LAZY_INITIALIZATION=true
      - JAVA_TOOL_OPTIONS=-Xss256k -XX:MaxMetaspaceSize=128m -XX:MaxRAMPercentage=60.0 -XX:+TieredCompilation -XX:TieredStopAtLevel=1 -XX:+UseSerialGC -Dnacos.logging.default.config.enabled=false
    deploy:
      resources:
        limits:
          memory: 448M
    networks:
      - algorithm-network

  # 文件服务
  algorithm-file-service:
    build:
      context: .
      dockerfile: algorithm-service/algorithm-file-service/Dockerfile
    image: algorithm-file-service:${ALOGRITHM_CLOUD_VERSION:-latest}
    container_name: algorithm-file-service
    restart: always
    ports:
      - "${FILE_SERVICE_PORT_EXTERNAL}:8085"
    environment:
      - SPRING_PROFILES_ACTIVE=prod
      - SPRING_CLOUD_NACOS_CONFIG_SERVER_ADDR=${NACOS_HOST}:${NACOS_PORT}
      - SPRING_CLOUD_NACOS_DISCOVERY_SERVER_ADDR=${NACOS_HOST}:${NACOS_PORT}
      - SPRING_CLOUD_NACOS_USERNAME=${NACOS_USERNAME}
      - SPRING_CLOUD_NACOS_PASSWORD=${NACOS_PASSWORD}
      - SPRING_MAIN_LAZY_INITIALIZATION=true
      - JAVA_TOOL_OPTIONS=-Xss256k -XX:MaxMetaspaceSize=128m -XX:MaxRAMPercentage=60.0 -XX:+TieredCompilation -XX:TieredStopAtLevel=1 -XX:+UseSerialGC -Dnacos.logging.default.config.enabled=false
    deploy:
      resources:
        limits:
          memory: 448M
    networks:
      - algorithm-network

  # 邮件服务
  algorithm-mail-service:
    build:
      context: .
      dockerfile: algorithm-service/algorithm-mail-service/Dockerfile
    image: algorithm-mail-service:${ALOGRITHM_CLOUD_VERSION:-latest}
    container_name: algorithm-mail-service
    restart: always
    ports:
      - "${MAIL_SERVICE_PORT_EXTERNAL}:8087"
    environment:
      - SPRING_PROFILES_ACTIVE=prod
      - SPRING_CLOUD_NACOS_CONFIG_SERVER_ADDR=${NACOS_HOST}:${NACOS_PORT}
      - SPRING_CLOUD_NACOS_DISCOVERY_SERVER_ADDR=${NACOS_HOST}:${NACOS_PORT}
      - SPRING_CLOUD_NACOS_USERNAME=${NACOS_USERNAME}
      - SPRING_CLOUD_NACOS_PASSWORD=${NACOS_PASSWORD}
      - SPRING_MAIN_LAZY_INITIALIZATION=true
      - JAVA_TOOL_OPTIONS=-Xss256k -XX:MaxMetaspaceSize=128m -XX:MaxRAMPercentage=60.0 -XX:+TieredCompilation -XX:TieredStopAtLevel=1 -XX:+UseSerialGC -Dnacos.logging.default.config.enabled=false
    deploy:
      resources:
        limits:
          memory: 448M
    networks:
      - algorithm-network

  # 通知服务
  algorithm-notification-service:
    build:
      context: .
      dockerfile: algorithm-service/algorithm-notification-service/Dockerfile
    image: algorithm-notification-service:${ALOGRITHM_CLOUD_VERSION:-latest}
    container_name: algorithm-notification-service
    restart: always
    ports:
      - "${NOTIFICATION_SERVICE_PORT_EXTERNAL}:8083"
    environment:
      - SPRING_PROFILES_ACTIVE=prod
      - SPRING_CLOUD_NACOS_CONFIG_SERVER_ADDR=${NACOS_HOST}:${NACOS_PORT}
      - SPRING_CLOUD_NACOS_DISCOVERY_SERVER_ADDR=${NACOS_HOST}:${NACOS_PORT}
      - SPRING_CLOUD_NACOS_USERNAME=${NACOS_USERNAME}
      - SPRING_CLOUD_NACOS_PASSWORD=${NACOS_PASSWORD}
      - SPRING_MAIN_LAZY_INITIALIZATION=true
      - JAVA_TOOL_OPTIONS=-Xss256k -XX:MaxMetaspaceSize=128m -XX:MaxRAMPercentage=60.0 -XX:+TieredCompilation -XX:TieredStopAtLevel=1 -XX:+UseSerialGC -Dnacos.logging.default.config.enabled=false
    deploy:
      resources:
        limits:
          memory: 448M
    networks:
      - algorithm-network

  # 搜索服务
  algorithm-search-service:
    build:
      context: .
      dockerfile: algorithm-service/algorithm-search-service/Dockerfile
    image: algorithm-search-service:${ALOGRITHM_CLOUD_VERSION:-latest}
    container_name: algorithm-search-service
    restart: always
    ports:
      - "${SEARCH_SERVICE_PORT_EXTERNAL}:8084"
    environment:
      - SPRING_PROFILES_ACTIVE=prod
      - SPRING_CLOUD_NACOS_CONFIG_SERVER_ADDR=${NACOS_HOST}:${NACOS_PORT}
      - SPRING_CLOUD_NACOS_DISCOVERY_SERVER_ADDR=${NACOS_HOST}:${NACOS_PORT}
      - SPRING_CLOUD_NACOS_USERNAME=${NACOS_USERNAME}
      - SPRING_CLOUD_NACOS_PASSWORD=${NACOS_PASSWORD}
      - SPRING_MAIN_LAZY_INITIALIZATION=true
      - JAVA_TOOL_OPTIONS=-Xss256k -XX:MaxMetaspaceSize=128m -XX:MaxRAMPercentage=60.0 -XX:+TieredCompilation -XX:TieredStopAtLevel=1 -XX:+UseSerialGC -Dnacos.logging.default.config.enabled=false
    deploy:
      resources:
        limits:
          memory: 448M
    networks:
      - algorithm-network

  # 日志服务
  algorithm-log-service:
    build:
      context: .
      dockerfile: algorithm-service/algorithm-log-service/Dockerfile
    image: algorithm-log-service:${ALOGRITHM_CLOUD_VERSION:-latest}
    container_name: algorithm-log-service
    restart: always
    ports:
      - "${LOG_SERVICE_PORT_EXTERNAL}:8086"
    environment:
      - SPRING_PROFILES_ACTIVE=prod
      - SPRING_CLOUD_NACOS_CONFIG_SERVER_ADDR=${NACOS_HOST}:${NACOS_PORT}
      - SPRING_CLOUD_NACOS_DISCOVERY_SERVER_ADDR=${NACOS_HOST}:${NACOS_PORT}
      - SPRING_CLOUD_NACOS_USERNAME=${NACOS_USERNAME}
      - SPRING_CLOUD_NACOS_PASSWORD=${NACOS_PASSWORD}
      - SPRING_MAIN_LAZY_INITIALIZATION=true
      - JAVA_TOOL_OPTIONS=-Xss256k -XX:MaxMetaspaceSize=128m -XX:MaxRAMPercentage=60.0 -XX:+TieredCompilation -XX:TieredStopAtLevel=1 -XX:+UseSerialGC -Dnacos.logging.default.config.enabled=false
    deploy:
      resources:
        limits:
          memory: 448M
    networks:
      - algorithm-network

  # AI 服务
  algorithm-ai-service:
    build:
      context: .
      dockerfile: algorithm-service/algorithm-ai-service/Dockerfile
    image: algorithm-ai-service:${ALOGRITHM_CLOUD_VERSION:-latest}
    container_name: algorithm-ai-service
    restart: always
    ports:
      - "${AI_SERVICE_PORT_EXTERNAL}:8089"
    environment:
      - SPRING_PROFILES_ACTIVE=prod
      - SPRING_CLOUD_NACOS_CONFIG_SERVER_ADDR=${NACOS_HOST}:${NACOS_PORT}
      - SPRING_CLOUD_NACOS_DISCOVERY_SERVER_ADDR=${NACOS_HOST}:${NACOS_PORT}
      - SPRING_CLOUD_NACOS_USERNAME=${NACOS_USERNAME}
      - SPRING_CLOUD_NACOS_PASSWORD=${NACOS_PASSWORD}
      - SPRING_MAIN_LAZY_INITIALIZATION=true
      - JAVA_TOOL_OPTIONS=-Xss256k -XX:MaxMetaspaceSize=128m -XX:MaxRAMPercentage=60.0 -XX:+TieredCompilation -XX:TieredStopAtLevel=1 -XX:+UseSerialGC -Dnacos.logging.default.config.enabled=false
    deploy:
      resources:
        limits:
          memory: 448M
    networks:
      - algorithm-network

networks:
  algorithm-network:
    external: true
