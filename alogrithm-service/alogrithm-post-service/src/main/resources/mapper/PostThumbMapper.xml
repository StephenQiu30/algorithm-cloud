<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.stephen.cloud.post.mapper.PostThumbMapper">

    <resultMap id="BaseResultMap" type="com.stephen.cloud.post.model.entity.PostThumb">
        <id property="id" column="id" jdbcType="BIGINT"/>
        <result property="postId" column="post_id" jdbcType="BIGINT"/>
        <result property="userId" column="user_id" jdbcType="BIGINT"/>
        <result property="createTime" column="create_time" jdbcType="TIMESTAMP"/>
        <result property="updateTime" column="update_time" jdbcType="TIMESTAMP"/>
    </resultMap>

    <sql id="Base_Column_List">
        id
        ,post_id,user_id,create_time,update_time
    </sql>

    <select id="listThumbPostByPage" resultType="com.stephen.cloud.post.model.entity.Post">
        SELECT p.*
        FROM post p
        JOIN (
        SELECT post_id
        FROM post_thumb
        WHERE user_id = #{thumbUserId}
        ) pt ON p.id = pt.post_id
        <if test="ew != null and ew.customSqlSegment != null and ew.customSqlSegment != ''">
            ${ew.customSqlSegment}
        </if>
    </select>

</mapper>
