# 接收各微服务 logback 通过 TCP 上报的 JSON 日志，写入 ES 供 Kibana 查询
input {
  tcp {
    port => 5000
    codec => json
  }
}
output {
  elasticsearch {
    hosts => ["elasticsearch:9200"]
    index => "logstash-%{+YYYY.MM.dd}"
  }
}
